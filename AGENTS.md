# Station Picker

## フォルダ構造

assetsフォルダに全国地方ごとのフォルダと海外フォルダがある。
`main.js`に地方・鉄道会社・路線・駅の生成用テンプレートと、HTML側操作用の関数、駅の抽選関数がある。
`{地方}/{鉄道会社}/{id}.js`に鉄道会社の路線ごとの駅一覧（小分けにしている場合もある）が書かれている。また同フォルダに路線や企業のロゴが保存されている。
`{地方}/{地方}.js`では、その地方の鉄道会社の情報とその路線の情報が記載されている。

海外の路線を保存する`oversea`では、上のフォルダ構造を`{国}/{鉄道会社}/{id}`として読み替えてもらう。

JRのフリーきっぷなど、複数社にまたがるタイプのきっぷを選択するのは大変なので、`assets/tickets`にチケットデータがある。
会社全体の場合・路線全体の場合・路線の一部区間の場合に対応している他、ほかのチケットに少し増えるだけのものであればappendで元のチケットを参照できる。

## 生成

`bundler.ts`が`assets`フォルダにある全てのjsファイルをバンドルし、`bundle.js`と`oversea-bundle.js`にminifyする。
`bun run build`でcssとjsの双方をバンドルできる。
